import { ActionInfo } from '@app/classes/actions';

export default class ActionClue extends ActionInfo {
    execute(): void {
        return;
    }

    getMessage(): string {
        return '';
    }
}
