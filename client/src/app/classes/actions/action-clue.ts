import { ActionInfo } from '@app/classes/actions/action-info';

export class ActionClue extends ActionInfo {
    execute(): void {
        return;
    }

    getMessage(): string {
        return '';
    }
}
