import { ActionInfo } from '@app/classes/actions';

export default class ActionHelp extends ActionInfo {
    execute(): void {
        return;
    }

    getMessage(): string {
        return '';
    }
}
