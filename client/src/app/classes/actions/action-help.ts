import { ActionInfo } from '@app/classes/actions/action-info';

export class ActionHelp extends ActionInfo {
    execute(): void {
        return;
    }

    getMessage(): string {
        return '';
    }
}
