import { ActionInfo } from '@app/classes/actions';

export default class ActionReserve extends ActionInfo {
    execute(): void {
        return;
    }

    getMessage(): string {
        return '';
    }
}
