import { ActionInfo } from '@app/classes/actions/action-info';

export class ActionReserve extends ActionInfo {
    execute(): void {
        return;
    }

    getMessage(): string {
        return '';
    }
}
